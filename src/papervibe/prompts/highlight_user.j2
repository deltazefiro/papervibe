Highlight approximately {{ highlight_ratio_percent }}% of the most important keywords and sentences in this chunk by wrapping them with \pvhighlight{...}:

{{ chunk }}

CRITICAL: Return the text EXACTLY as provided, with ONLY \pvhighlight{...} wrappers added around important keywords and sentences. Preserve all whitespace and formatting.
